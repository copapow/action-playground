name: Demo

on:
  pull_request:
    types: [labeled, synchronize, edited]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: copapow/javascript-action-example@main
        with:
          path: 'sample-path'
          major_label: 'major'
          minor_label: 'minor'
          patch_label: 'patch'
